# 智能合并与去重系统设计方案 (Agent C Integration)

**版本**: v1.0 (Draft)
**日期**: 2026-02-09
**目标**: 引入 Agent C (MergeBot) 解决多源数据合并时的“同名异实”、“异名同实”及层级对齐问题。

---

## 1. 核心问题分析

当前的 `MergerService` 采用静态规则（`Site + Type + Name` 完全匹配），存在以下缺陷：
1.  **层级丢失**: 无法区分 `A区-M1` 和 `B区-M1`，导致错误合并。
2.  **名称变体**: 无法识别 `M1` 和 `M-1` 为同一物体，导致数据重复。
3.  **遗址归一化**: 无法自动将 `良渚`、`良渚遗址`、`良渚古城` 归为同一个站点。

## 2. 系统架构升级

### 2.1 新增角色：Agent C (Deduplication Specialist)
*   **定位**: 数据治理专家/归户仲裁员。
*   **职责**: 仅在合并阶段介入，负责比对“新实体”与“库中候选实体”，判定是**合并 (Merge)** 还是 **新增 (Create)**。
*   **部署**: 加入 Agent 资源池，通过 `AgentManager` 管理，支持并发控制。

### 2.2 工作流程设计 (Enhanced Merge Flow)

合并流程将分为两个阶段：**Site 级归户** 和 **Entity 级归户**。

#### 阶段一：遗址 (Site) 归户
在处理任务前，先确定该任务属于哪个主遗址。
1.  **提取**: 从任务中提取遗址名称（如 "良渚"）。
2.  **检索**: 在 `master_entities` 中搜索相似名称（如 "良渚遗址", "良渚古城"）。
3.  **仲裁 (Agent C)**:
    *   如果有高置信度候选，调用 Agent C 判断："良渚" 和 "良渚遗址" 是同一个地方吗？
    *   **结果**: 返回 Master Site ID。

#### 阶段二：实体 (Entity) 归户 (递归处理)
采用 **"Parent-First" (先父后子)** 的策略，确保层级路径正确。

1.  **锁定**: 对目标 Site 申请 **分布式锁** (基于 Redis 或 DB)，确保同一遗址的数据串行合并，防止竞态条件。
2.  **遍历**: 按层级深度（Site -> Subarea -> Feature -> Artifact）遍历任务中的实体。
3.  **判重逻辑**:
    *   **Step 1: 精确匹配 (Fast Path)**: 检查 `Name` + `Type` + `Parent ID` 是否完全一致。是 -> 合并。
    *   **Step 2: 候选召回 (Recall)**: 在父节点下，搜索名称相似的实体。
    *   **Step 3: 智能仲裁 (Agent C)**:
        *   输入: 新实体信息 (含 Agent A 的 Tips)、候选实体列表。
        *   Prompt: "新实体 'M1' (位于 A区) 与 库中 'M-1' (位于 A区) 是同一个吗？"
        *   输出: `MATCH_ID` 或 `NEW`。
4.  **执行**: 根据仲裁结果执行 Update 或 Insert。

---

## 3. Agent 设计

### 3.1 Agent A: 增强上下文 (Context Provider)
需要修改 Agent A 的 Prompt，使其在输出结构化数据时，更明确地描述“身份特征”。

*   **新增指令**: 在 `entity_specific_tips` 中增加 "Identity Context"（身份指纹）。
*   **示例**:
    *   *原*: "M1, looted."
    *   *新*: "Identity: Excavated in 1986, located in Zone A. Condition: looted."

### 3.2 Agent C: 系统提示词 (System Prompt)

```markdown
# Role
You are an expert Archaeological Data Steward (考古数据归户专家).
Your goal is to determine if a "New Entity" extracted from a report is the same as any existing "Candidate Entities" in the Master Database.

# Input Format
1. **New Entity**:
   - Name: "M1"
   - Type: "FEATURE"
   - Path: "Liangzhu Site > Zone A"
   - Context Tips: "Excavated in 1986, rectangular pit."
2. **Candidate Entities** (List):
   - [ID: 101] Name: "M-1", Path: "Liangzhu > Zone A", Context: "1986 excavation."
   - [ID: 102] Name: "M1", Path: "Liangzhu > Zone B", Context: "2005 excavation."

# Judgment Rules
1. **Hierarchy Matters**: Entities in different Zones (e.g., Zone A vs Zone B) are DIFFERENT, even if names are identical.
2. **Name Variance**: Accept minor variances (e.g., "M1" == "M-1", "Pottery 1" == "1:1") if Context matches.
3. **Conflict Resolution**: If Context contradicts (e.g., different excavation years), they are DIFFERENT.

# Output Format (JSON)
{
  "decision": "MATCH" | "NEW",
  "target_master_id": "UUID (if MATCH) or null",
  "reason": "Brief explanation."
}
```

---

## 4. 改造实施计划

### 4.1 代码变更
1.  **`src/services/merger_service.py`**:
    *   重构 `merge_task` 方法，实现 "Site归户 -> 加锁 -> 递归实体归户" 流程。
    *   引入 `AgentManager` 调用 Agent C。
2.  **`src/prompts/agent_a_structure.md`**:
    *   增加关于 "Identity Context" 的指令。
3.  **`src/prompts/agent_c_dedup.md` (New)**:
    *   创建 Agent C 的 Prompt 文件。
4.  **`src/db/models.py`**:
    *   `SysTask` 可能需要增加 `merge_status` 字段来追踪详细进度。

### 4.2 并发控制
*   **方案**: 使用 Python `asyncio.Lock` (单机) 或 数据库行锁 (多机)。鉴于目前是单机部署，建议在 `MergerService` 中维护一个 `Dict[site_name, Lock]`，确保同一遗址的合并任务串行化。

### 4.3 资源配置
*   需要在 `config.py` 和 `.env` 中增加 `COZE_BOT_ID_C`。
