# 文物数据抽取系统 - 详细操作手册

本手册提供使用本系统的分步指南，涵盖GUI和CLI两种模式。

## 模式一: 图形用户界面 **(推荐给大多数用户)**

这是最直观的使用方式，无需编写代码或命令行指令。

### 1. 启动GUI应用
1.  打开您的终端 (Terminal) 或命令提示符 (Command Prompt)。
2.  导航到本项目所在的目录。
    ```bash
cd /path/to/your/yuki-cidoc-proj
    ```
3.  安装Streamlit（如果尚未安装）。
    ```bash
pip install streamlit
    ```
4.  安装其他依赖。
    ```bash
pip install pandas openpyxl
    ```
5.  启动应用。
    ```bash
streamlit run gui/app.py
    ```
6.  应用将在您的默认浏览器中自动打开一个新的标签页。如果未自动打开，请复制终端中显示的URL（通常是 `http://localhost:8501`）并在浏览器中粘贴。

### 2. 配置LLM服务（一次性设置）
1.  在浏览器打开的页面左侧边栏。找到 **"LLM 服务"** 部分。
2.  在 "API Key" 输入框中，输入您从 [Anthropic Console](https://console.anthropic.com/) 获取的API密钥。
3.  点击 **"💾 保存配置"** 按钮。系统会显示一个绿色的成功消息，确认您的密钥已安全地保存在 `config.json` 文件中。这个步骤只需完成一次。

### 3. 执行数据抽取
1.  在左侧边栏的 **"选择报告和模板"** 部分，从下拉菜单中选择您要处理的报告文件（例如 `full.md`）和对应的数据结构模板文件（例如 `structure_v2.xlsx`）。
2.  在主页面，切换到 **"🔍 数据抽取"** 选项卡。
3.  点击 **"开始抽取"** 按钮。
4.  系统将调用后端的抽取引擎，使用LLM分析报告，并将结构化数据存储到数据库中。完成后，您会看到一个成功消息。

### 4. 查看和导出结果
1.  在主页面，切换到 **"📊 数据库浏览"** 选项卡。
2.  从 **"选择数据表"** 的下拉菜单中，选择一个数据表（例如 `artifacts`）。
3.  数据以表格形式显示在下方。
4.  点击 **"📥 导出为 CSV"** 按钮，将当前表的所有数据下载为CSV文件，用于在Excel等程序中进一步分析。

---

## 模式二: 命令行界面 **(适合自动化和高级用户)**

此模式适合将抽取任务集成到脚本或批处理流程中。

### 1. 准备环境
1.  确保已安装Python（>=3.8）。
2.  在项目根目录，创建并激活一个虚拟环境（推荐）。
    ```bash
python -m venv venv
source venv/bin/activate  # Linux/macOS
# 或
venv\Scripts\\activate    # Windows
    ```
3.  安装所需依赖库。
    ```bash
pip install openpyxl pandas
    ```
4.  按照GUI模式中的第2步，将您的LLM API Key填入 `config.json` 文件并保存。

### 2. 执行抽取命令
运行 `main.py` 脚本，提供必要的路径参数。

```bash
python main.py -r reports/full.md -t templates/structure_v2.xlsx -d database/my_output.db
```

**参数说明**：
-   `-r` 或 `--report`：指定要处理的报告文件的路径。
-   `-t` 或 `--template`：指定要使用的数据结构模板文件的路径。
-   `-d` 或 `--database`：指定输出数据库文件的路径（可选，如果省略，默认为 `database/artifacts.db`）。

脚本运行完毕后，您可以在指定路径找到包含抽取结果的SQLite数据库文件。

### 3. 访问数据库结果
您可以使用任何支持SQLite的数据库浏览器（如 [DB Browser for SQLite](https://sqlitebrowser.org/)）来打开和查询生成的 `.db` 文件，或使用Python的`sqlite3`库进行编程化访问。